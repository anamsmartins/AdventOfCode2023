; i < 5; i++) {

    //     // Find the position of the characters in the order array
    //     int index1 = strchr(order, handA->cards[i]) - order;
    //     int index2 = strchr(order, handA->cards[i]) - order;

    //     // Compare the positions
    //     if (index1 < index2) {
    //         return -1;
    //     } else if (index1 > index2) {
    //         return 1;
    //     }
    // }